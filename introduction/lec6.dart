// TYPE CONVERTION

main() {
  var one = int.parse('1');
  assert(one == 1);

// php de ki isset gibi galiba

  var onePointOne = double.parse('1.1');
  assert(onePointOne == 1.1);

  // there is no error
}
